export default async () => {
  console.log("E2E tests completed, cleaning up...");
  // Playwright handles webServer process cleanup automatically
  // This is a placeholder for any custom cleanup if needed
};
